# Contributing

Heuristics should stay transparent and easy to audit. Prefer adding focused tests with representative JSON payloads before changing classifier behavior.

Run:

```bash
python -m unittest discover -s tests
```

Release instructions live in [PUBLISHING.md](PUBLISHING.md).
